import { Button } from "@/components/ui/button";

export function ClassForm({ action, initial, submitLabel = "Salvar turma" }: { action: (data: FormData) => Promise<void>; initial?: { name: string; description?: string | null; status: "ACTIVE" | "INACTIVE" }; submitLabel?: string }) {
  return <form action={action} className="mt-6 space-y-5"><div><label className="field-label" htmlFor="name">Nome</label><input id="name" name="name" required maxLength={120} defaultValue={initial?.name} className="mt-1.5 block h-10 w-full px-3" /></div><div><label className="field-label" htmlFor="description">Descrição <span className="text-[var(--muted-foreground)]">(opcional)</span></label><textarea id="description" name="description" rows={4} maxLength={5000} defaultValue={initial?.description ?? ""} className="mt-1.5 block w-full px-3 py-2" /></div><div><label className="field-label" htmlFor="status">Status</label><select id="status" name="status" defaultValue={initial?.status ?? "ACTIVE"} className="mt-1.5 block w-full px-3 sm:w-56"><option value="ACTIVE">Ativa</option><option value="INACTIVE">Inativa</option></select></div><div className="flex justify-end border-t border-[var(--border)] pt-5"><Button type="submit">{submitLabel}</Button></div></form>;
}
